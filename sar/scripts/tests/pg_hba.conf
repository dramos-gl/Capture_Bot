# ===========================================================================
# CONFIGURACIÓN DE ACCESO CONTROLADO (PG_HBA.CONF) - SISTEMA SAR V2.0
# ===========================================================================
# Ubicación típica: C:\Program Files\PostgreSQL\<Versión>\data\pg_hba.conf
#
# Regla de Oro: Primero se valida la coincidencia de arriba hacia abajo.
# La primera regla que coincida con la conexión es la que se aplica.
# ===========================================================================

# ---------------------------------------------------------------------------
# 1. CONEXIONES LOCALES (Requerido para el Servidor y la API local en el Servidor 10.11.8.151)
# ---------------------------------------------------------------------------
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# ---------------------------------------------------------------------------
# 2. CONEXIÓN DEL EQUIPO CLIENTE DE PRUEBAS (10.11.8.108)
# ---------------------------------------------------------------------------
# Permite conexión del equipo cliente 10.11.8.108 a la base de datos db_sar
# TYPE  DATABASE        USER            ADDRESS                 METHOD
host    db_sar          postgres        10.11.8.108/32          scram-sha-256

# ---------------------------------------------------------------------------
# Nota de Seguridad para Despliegue Final (Fase 3 completa):
# Una vez migrados todos los módulos a la API REST, esta sección remota deberá 
# ser comentada o eliminada para asegurar un aislamiento completo de PostgreSQL.
# ===========================================================================
